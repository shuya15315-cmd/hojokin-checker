# 引き渡し手順（本番PCでの設定）

このツール一式を、**別のPC（Claude Code が入っている本番PC）**に渡して運用するための手順です。
「開発・検証は今のPC」「実際の自動更新＆Teams共有は本番PC」という役割分担を想定しています。

---

## 全体像（GitHub Pages ＋ Teamsタブ方式）

```
[本番PC] Claude Code入り。このツールを GitHub リポジトリとして管理
   └ 毎週月曜8:00 自動更新 → data.js 最新化 → 自動で git push
        └→ GitHub Pages が最新を配信
[Teams]  チャンネルに「Webサイトタブ」を設置（GitHub PagesのURL）
[みんな] Teamsのタブを開くだけ（Claude不要・DL不要・常に最新）
```

---

## 手順（本番PC側で作業）

### 1. 前提を確認
- 本番PCに **Claude Code がインストール済み・ログイン済み** であること。
- 本番PCで **Teams（または OneDrive/SharePoint）の対象フォルダが同期** されていること
  （＝月次資料の取込ツールと同じ考え方。フォルダがPC上にローカルとして見える状態）。

### 2. フォルダを置く場所
このツールのフォルダ（`hojokin-checker` 一式）を、本番PCの好きな場所に置きます（同期フォルダである必要はありません）。
- 例：`C:\Users\(ユーザー名)\tools\hojokin-checker\`
- GitHub連携（下記「Teamsで見られるように」）をしておくと、更新が自動でGitHub Pagesへ反映され、Teamsタブが最新になります。

### 3. セットアップを実行
コピーしたフォルダの中の **`セットアップ.bat`** をダブルクリック。
- 「毎週月曜 8:00 の自動更新タスク」が登録されます。
- デスクトップに閲覧用ショートカットが作られます。
- ※うまくいかない時は `セットアップ.bat` を右クリック →「管理者として実行」。

### 4. 動作確認
- フォルダの **`今すぐ更新.bat`** をダブルクリック → 数分待つ → `index.html` を開いて中身が最新になっていればOK。
- （`logs` フォルダに実行記録が残ります）

### 5. 開発PC（今のPC）側の後始末
- 今のPCでは自動更新が二重に走らないよう、タスクスケジューラで
  **「補助金税制チェッカー_自動更新」を無効化 または 削除** してください。

---

## Teamsで“見られる”ようにする（GitHub Pages ＋ Teamsタブ）

### A. GitHubリポジトリを用意して公開（最初の1回だけ）
1. GitHubで新しいリポジトリを作る（例：`hojokin-checker`）。**Public** でOK（このツールは公開情報のみ）。
2. 本番PCで、このフォルダをそのリポジトリにつなげて初回アップロード：
   ```
   git remote add origin https://github.com/(ユーザー名)/hojokin-checker.git
   git push -u origin main
   ```
   （このフォルダは既に git 管理済みなので `git init` は不要です。）
3. GitHubのリポジトリ → **Settings → Pages** → 「Deploy from a branch」→ Branch: `main` / フォルダ: `/ (root)` → Save。
4. 数分後、`https://(ユーザー名).github.io/hojokin-checker/` で表示されればOK。これが**公開URL**です。

### B. Teamsのチャンネルにタブを追加
1. 対象チャンネル上部の「＋（タブを追加）」→ **「Webサイト」** を選ぶ。
2. 名前：「補助金・税制改正チェッカー」、URL：上の**公開URL**を貼る → 保存。
3. これで、チャンネルのタブを開くだけで**全員が常に最新のダッシュボード**を見られます（DL・同期・Claude すべて不要）。

### C. あとは自動
- 本番PCの週次自動更新が走ると、`update.ps1` が **自動で `git push`** します（remote設定後は自動）。
- GitHub Pages が更新され、**Teamsタブも自動で最新**になります。

> ⚠ 「毎週の通知はうるさい」問題は、この方式では**通知ゼロ**（タブで見たい時に見る＝プル）なので発生しません。
> もし「新着があった週だけ通知」も欲しくなったら、別途 Power Automate（Workflows）で対応できます。

---

## （任意・将来）月曜8:30に「新着があった時だけ」通知する

毎週ではなく「新しい補助金・改正が増えた週だけ」Teamsに投稿したい場合：
- Teams の従来の「Incoming Webhook」は廃止方向のため、**Power Automate（Teamsの「Workflows」）**でチャンネル投稿する形になります。
- 「新着」の件数を出せるようにしてあるので、「新着0の週は投稿しない」条件も作れます。
- 必要になったら、この仕組みづくりも対応します（声をかけてください）。

---

## 自動更新の時刻変更・停止（本番PC）
Windowsの「タスクスケジューラ」→ **「補助金税制チェッカー_自動更新」**
- 時刻変更：右クリック →「プロパティ」→「トリガー」
- 一時停止：右クリック →「無効」／ 削除：右クリック →「削除」

---

## 注意
- このツールは**公開情報を読みに行くだけ**で、会社の情報を外に出しません。
- 内容は**早わかり用**です。申請・判断の前に、必ず各カードの🔗出典（公式）と税理士等でご確認ください。
