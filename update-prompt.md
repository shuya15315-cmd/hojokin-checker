# data.js 自動更新の指示書（Claudeが読んで実行する）

あなたは日本の補助金・税制の調査アシスタントです。このフォルダの `data.js` を、公式・一次情報をもとに**最新の内容へ書き換える**のがゴールです。

## 最重要ルール（情報漏洩の防止）
- **公開情報を「取りに行く」だけ**。WebSearch と WebFetch で公開ページを読むこと。
- 検索クエリは一般的な公開語のみ（例:「IT導入補助金 2026 公募」）。**会社名・個人情報・社内データを検索やページに一切含めない/送らない**。
- 使ってよいツールは WebSearch / WebFetch / Read / Write / Edit / Glob / Grep のみ。

## 手順
1. まず `data.js` を Read し、既存の構造とデータ（フィールド名）を把握する。
2. 今日の日付を確認する（環境の現在日付を使う。以下「本日」と呼ぶ。形式は YYYY-MM-DD）。
3. 下記4分野の**補助金・助成金**と、直近の**税制改正**を、公式サイト中心に調べる。
4. 既存データを土台に、次を行う：
   - 新しい制度・公募を**追加**する。追加した項目には `追加日` に「本日(YYYY-MM-DD)」を入れる。
   - すでにある項目の `追加日` は**変更しない**（初出の日付を保持。元々無ければ付けなくてよい）。
   - 既存項目の締切・金額・要件・ステータスを**最新化**。
   - 締切が本日より前のものは `ステータス` を「終了」にする（履歴として残す）。ただし締切が**1年以上前**のものは削除してよい。
   - **申請受付の開始日がまだ来ていないもの**（公募要領は公開済みでも受付前）は、締切日が未来でも `ステータス` を「準備中(公募待ち)」にする（＝「募集中」にしない）。受付開始日は `公募期間` に明記する。
   - 内容が変わっていない項目はそのまま残す（ただし確認できたものは `取得日` を本日に更新）。
5. `data.js` を**同じ構造のまま**上書き保存する（下記スキーマ厳守）。
6. `window.メタ` を更新する：
   - `前回更新` に、**更新前の**（今の）`最終更新` の日付を入れる（＝「新着」判定の基準になる）。
   - `最終更新` を「本日(YYYY-MM-DD)」にする。
   - `次回更新予定` は「毎週月曜（自動更新）」のまま。

## 調べる分野（業種タグ）
- `中小企業全般`（IT導入補助金、ものづくり補助金、小規模事業者持続化補助金、省力化投資補助金、事業承継・引継ぎ補助金、賃上げ関連の助成金 など）
- `金融・投資・創業`（創業支援、スタートアップ支援、新事業進出/事業再構築系の後継 など）
- `不動産・建設`（省エネ改修、住宅・建築系、建設DX、GX/脱炭素投資 など）
- `介護・福祉・見守り`（介護テクノロジー導入支援、ICT導入支援、処遇改善、見守り機器補助 など）

## 情報源の優先順位
1. 一次情報：各省庁（経産省・中小企業庁・厚労省・国交省・環境省・国税庁・財務省）、ミラサポplus、J-Net21、各補助金の公式事務局、自治体公式、WAM NET。
2. 補足：信頼できる税理士法人・会計事務所の解説（ただし**数値・期限は必ず一次情報で裏取り**）。

## 新しいものの見つけ方（毎回必ず実施）
「決まったURLを見るだけ」にしないこと。以下を毎回行い、**新設・新規公募・新しい改正を能動的に探す**：
1. **キーワード検索（WebSearch）で"新規"を探す。** 「補助金 2026 新設 公募開始」「令和8年度 税制改正 新設」「(各業種) 補助金 新規 2026」「(省庁名) 報道発表 補助金」など、"新設""新規""創設""拡充""公募開始""報道発表"を絡めて複数パターン検索する。
2. 下の**「定点チェックの一覧・新着ページ」を確認し、前回データに無い項目を拾う**（新設ものはまずこうした一覧・新着に載る）。
3. 見つけた新規は、公式の詳細ページで裏取りしてから追加する（`追加日` は本日）。

## 定点チェックする「一覧・新着」ページ（毎回見る）
補助金・支援：
- ミラサポplus 補助金・支援制度一覧: https://mirasapo-plus.go.jp/subsidy/
- J-Net21 支援情報ヘッドライン（新着の補助金・助成金）: https://j-net21.smrj.go.jp/snavi/
- jGrants（補助金電子申請）公募一覧: https://www.jgrants-portal.go.jp/
- WAM NET 福祉医療の助成金・補助金（介護・福祉系）: https://www.wam.go.jp/content/wamnet/pcpub/top/josei/

税制：
- 財務省 税制改正: https://www.mof.go.jp/tax_policy/tax_reform/
- 国税庁 新着情報: https://www.nta.go.jp/information/
- 経済産業省 税制: https://www.meti.go.jp/main/zeisei/

自治体・その他：
- 対象業種に関係しそうなら、各都道府県・政令市の「補助金」ページや各省庁の「報道発表」ページも確認する。

※これらのページ自体が WebFetch で開けない場合（403等）は、WebSearch の検索結果や別の公式ミラー（ミラサポplus／中小機構／J-Net21 等）で裏取りする。漏れを疑ったら検索パターンを変えて再検索する。

## 正確性のルール（最重要）
- **実在し、出典URLで裏取りできる項目だけ**を載せる。締切・金額・要件を推測で作らない。
- 不確かな値は該当フィールドに `要確認` と書き、`備考` に状況（例:「次回公募待ち」）を記す。
- **各項目に最低1つ、公式の `出典` URL**を付ける。
- 自治体ごとに実施される補助（介護テクノロジー等）は、概要に「多くの自治体で実施。窓口は各自治体」と明記し、代表的な自治体の出典を1つ付ける。`締切日` は `null` でよい。

## data.js のスキーマ（この形を厳守）

```js
window.メタ = {
  最終更新: "YYYY-MM-DD",
  前回更新: "YYYY-MM-DD",   // 更新前の最終更新日を入れる（新着判定の基準）
  次回更新予定: "毎週月曜（自動更新）",
  注記: "この一覧は早わかり用です。申請・判断の前に必ず出典（公式サイト）と税理士等でご確認ください。"
};

window.補助金データ = [
  {
    id: "半角英数のユニークID",
    名称: "制度名",
    実施機関: "省庁/事務局",
    業種タグ: ["中小企業全般"],            // 上記4タグから該当を（複数可）
    概要: "非専門家にも分かる2〜4文",
    "対象・要件": ["箇条書き", "..."],
    補助額: "例: 最大450万円 / 要確認",
    補助率: "例: 1/2〜3/4 / 要確認",
    公募期間: "YYYY-MM-DD〜YYYY-MM-DD / 通年 / 未定(次回公募待ち)",
    締切日: "YYYY-MM-DD",                  // ソート用。通年/不明は null
    ステータス: "募集中",                   // 募集中 / 締切間近 / 準備中(公募待ち) / 終了
    出典: [{ タイトル: "公式名", url: "https://..." }],
    追加日: "YYYY-MM-DD",  // 今回新しく追加した項目のみ本日の日付。既存項目は初出日を保持
    取得日: "YYYY-MM-DD",
    備考: ""
  }
];

window.税制改正データ = [
  {
    id: "半角英数のユニークID",
    制度名: "制度名",
    改正年度: "令和8年度(2026年度)",
    区分: "法人税",                        // 法人税/所得税/消費税/資産税/手続き・電子化 など
    概要: "非専門家にも分かる2〜4文",
    要点: ["・要点", "・実務でどう効くか"],
    "対象業種・対象者": ["中小企業全般", "資本金1億円以下の法人 等"],
    適用時期: "例: 2026年4月1日以後開始事業年度 / 要確認",
    影響度: "大",                          // 大 / 中 / 小
    出典: [{ タイトル: "国税庁/財務省 等", url: "https://..." }],
    追加日: "YYYY-MM-DD",  // 今回新しく追加した項目のみ本日の日付。既存項目は初出日を保持
    取得日: "YYYY-MM-DD",
    備考: ""
  }
];
```

## 仕上げ
- `data.js` は上記3つの `window.` 変数だけを含む、そのまま実行できる正しいJavaScriptにする（末尾カンマ等の文法ミスに注意）。
- フィールド名（`対象・要件` `対象業種・対象者` など）は**一字一句この通り**にする（画面表示がこれに依存している）。
- 補助金は合計25〜40件、税制改正は8〜16件を目安に、質を優先して収録する。
- 作業後、変更点の要約（追加/更新/終了の件数）を1〜3行で述べて終了する。
